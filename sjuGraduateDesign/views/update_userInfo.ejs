<!DOCTYPE html>
<html>

	<head>
		<meta charset="utf-8">
		<title>壹物流</title>
		<meta name="viewport" content="width=device-width, initial-scale=1,maximum-scale=1,user-scalable=no">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<meta name="apple-mobile-web-app-status-bar-style" content="black">

		<!--标准mui.css-->
		<link rel="stylesheet" href="/stylesheets/mui.min.css">
		<style>
		</style>
	</head>

	<body>
		<header class="mui-bar mui-bar-nav">
			<a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
			<h1 class="mui-title">用户信息完善</h1>
		</header>
		<div class="mui-content">
			<!-- <div class="mui-content-padded"> -->
				<form class="mui-input-group">
					<!-- <div class="mui-input-row">
						<label>Input</label>
						<input type="text" placeholder="普通输入框">
					</div> -->
					<div class="mui-input-row">
						<label>联系人</label>
						<input type="text" class="mui-input-clear" placeholder="用于收货十分重要!">
					</div>
					<div class="mui-input-row">
						<label>手机号</label>
						<input type="text" class="mui-input-clear" placeholder="用于收货十分重要!">
					</div>
					<div class="mui-input-row">
						<label>店铺名称</label>
						<input type="text" class="mui-input-clear" placeholder="用于收货十分重要!">
					</div>
					<div class="mui-input-row">
						<label>地址</label>
						<input type="text" class="mui-input-clear" placeholder="用于收货十分重要!">
					</div>
					<div class="mui-input-row">
						<label>代理编号</label>
						<input type="text" class="mui-input-clear" placeholder="可拨打9527咨询总公司客服">
					</div>
					<div class="mui-input-row">
						<label>店铺编号</label>
						<input type="text" class="mui-input-clear" placeholder="可拨打9527咨询总公司客服">
					</div>
					<div class="mui-input-row">
						<label>备注</label>
						<input type="text" class="mui-input-clear" placeholder="">
					</div>

					<div class="mui-button-row">
						<button id="submit_info" type="button" class="mui-btn mui-btn-primary" onclick="return false;">确认</button>&nbsp;&nbsp;
					</div>
				</form>
			<!-- </div> -->
		</div>
		<script src="/javascripts/mui.min.js"></script>
		<script src="/javascripts/jquery-1.7.2-min.js"></script>
		<script>
		//地址栏参数截取正则
			function GetQueryString(name)
			{
			     var reg = new RegExp("(^|&)"+ name +"=([^&]*)(&|$)");
			     var r = decodeURI(window.location.search).substr(1).match(reg);
			     if(r!=null)return  unescape(r[2]); return null;
			}

			var phoneNumber = GetQueryString("phoneNumber");
			$("body").find("input").eq(1).val(phoneNumber);

			$("#submit_info").on("tap", function(){
				var contact = $("body").find("input").eq(0).val();
				var storeName = $("body").find("input").eq(2).val();
				var address = $("body").find("input").eq(3).val();
				var userCode = $("body").find("input").eq(4).val();
				var storeCode = $("body").find("input").eq(5).val();
				var notice = $("body").find("input").eq(6).val();
				
				$.ajax({
				type:"POST",
				url:"/regist/updateUserInfo",
				dataType:"json",
				async:true,
				data:{
					contact: contact,
					phoneNumber: phoneNumber,
					storeName: storeName,
					address: address,
					userCode: userCode,
					storeCode: storeCode,
					notice: notice
				},
				success:function(result){
					console.log(result);

				},
				error:function(err){

				}

				});

			});
			
			
		</script>
	</body>

</html>
