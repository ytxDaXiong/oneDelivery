<!DOCTYPE html>
<html>

	<head>
		<meta charset="utf-8">
		<title>壹物流</title>
		<meta name="viewport" content="width=device-width, initial-scale=1,maximum-scale=1,user-scalable=no">
		<meta name="apple-mobile-web-app-capable" content="yes">
		<meta name="apple-mobile-web-app-status-bar-style" content="black">
		<!--标准mui.css-->
		<link rel="stylesheet" href="/stylesheets/mui.min.css">
		<style>
		</style>
	</head>

	<body>
		<header class="mui-bar mui-bar-nav">
			<a class="mui-action-back mui-icon mui-icon-left-nav mui-pull-left"></a>
			<h1 class="mui-title">用户信息与地址修改</h1>
		</header>
		<div class="mui-content">
			<!-- <div class="mui-content-padded"> -->
				<form class="mui-input-group">
					<div class="mui-input-row">
						<label>联系人</label>
						<input type="text" class="mui-input-clear" value=<%= result.contact%> placeholder="联系人">
					</div>
					<div class="mui-input-row">
						<label>手机号</label>
						<input type="text" class="mui-input-clear" value=<%= result.tel%> placeholder="手机号">
					</div>
					<div class="mui-input-row">
						<label>店铺名称</label>
						<input type="text" class="mui-input-clear" value=<%= result.storeName%> placeholder="店铺名称">
					</div>
					<div class="mui-input-row">
						<label>地址</label>
						<input type="text" class="mui-input-clear" value=<%= result.address%> placeholder="地址">
					</div>
					<div class="mui-input-row">
						<label>备注</label>
						<input type="text" class="mui-input-clear" placeholder="">
					</div>
				</form>

				<div class="mui-button-row">
					<button id="submit_info" type="button" class="mui-btn mui-btn-primary mui-btn-block">确认</button>
				</div>
			<!-- </div> -->
		</div>
		<script src="/javascripts/mui.min.js"></script>
		<script src="/javascripts/jquery-1.7.2-min.js"></script>
		<script>

			$("#submit_info").on("tap", function(){
				var contact = $("body").find("input").eq(0).val();
				var tel = $("body").find("input").eq(1).val();
				var storeName = $("body").find("input").eq(2).val();
				var address = $("body").find("input").eq(3).val();
				var notice = $("body").find("input").eq(4).val();
				
				$.ajax({
				type:"POST",
				url:"/users/userInfo&address/infoModify",
				dataType:"json",
				async:true,
				data:{
					contact: contact,
					tel: tel,
					storeName: storeName,
					address: address,
					notice: notice
				},
				success:function(result){
					console.log(result);
					mui.alert(result.message);
				},
				error:function(err){
					console.log(err);
				}

				});

			});
			
			
		</script>
	</body>

</html>
